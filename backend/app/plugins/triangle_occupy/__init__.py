"""triangle_occupy 玩法插件包（todo 13）。

- ``manifest.json``: 插件元数据（注册表扫描契约）。
- ``plugin.py``: :class:`TriangleOccupyPlugin`（GameplayPlugin 适配层）。
- ``controller/``: demo 赛时控制器规则引擎（原样复制，零改动）。
- ``config/rules.json``: 规则配置（``controller/rules.py`` 按相对路径加载）。
"""
